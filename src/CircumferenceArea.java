public class CircumferenceArea {

    static double calcCircumference(double rad){
        return Math.PI * rad * 2;
    }
    static double calcArea(double rad){
        return Math.PI * Math.sqrt(rad);
    }
}
